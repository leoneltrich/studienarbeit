\section{Literature Review}

In the literature review we conducted, we found multiple research papers exploring the use of machine learning for translating natural language to commands **isnert refs**. However, none of them accurately capture the use cases we are trying to solve in our work. 

% Differences: Use LLMs, Are not extensible (require re-training), do not work with internal tools, aim to provide full commands instead of just retrieving the tool and options and filling args with placeholders which is what we do
\subsection{Related Works}