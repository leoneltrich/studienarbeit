\section{Systems Design And Implementation}

\subsection{Requirements}

% Database:
%- Tabelle für Tool beschreibungen
%    - Tool name (ID)
%    - Tool Beschreibung (TEXT)
%    - Tool Beschreibung Vector (BLOB)
%- Tabelle für tool option Beschreibungen
%    - option (ID)
%    - Tool Name (ID)
%    - Tool Option beschreibung (TEXT)
%    - Tool Option Beschreibung Vector (BLOB)

%Pipeline Design \& Systemablaufdiagramm

%2 systeme, eine lightweight api die das modell lädt und embeddings macht falls app registriert wird und das cli system

Before defining the functional behaviors and quality attributes of the system, it is necessary to establish the immutable physical and operational boundaries within which the software must execute. These environmental constraints dictate the hardware baseline for all subsequent performance evaluations and formally enforce the strict offline, zero-dependency philosophy of the proposed architecture. The fundamental constraints governing the local execution environment are detailed in Table \ref{tab:env_constraints}.

\begin{table}[htbp]
    \centering
    \caption{Hardware and Environmental Constraints}
    \label{tab:env_constraints}
    \renewcommand{\arraystretch}{1.3} % Adds slightly more space between rows
    \begin{tabularx}{\textwidth}{@{} l l X @{}}
        \toprule
        \textbf{ID} & \textbf{Constraint} & \textbf{Description} \\
        \midrule
        ENV-1 & Target Hardware & All performance metrics must be achieved on a baseline Proxmox VM running a Linux based operating system, configured with 4 vCPUs (AMD Ryzen 7 5700X architecture), 8GB of 2133MHz DDR4 RAM, and a Samsung SSD 970 Evo Plus NVMe drive. \\
        ENV-2 & Offline Isolation & The system must function completely offline. It must not initiate any outbound network requests under any circumstances. \\
        ENV-3 & Zero Third-Party Daemon & The system must be entirely self-contained. While the system's own architecture may utilize background processes, it must not require the installation or execution of third-party standalone server software. \\
        ENV-4 & Headless Execution & The target environment is strictly headless. The system must not rely on or require graphical windowing systems or desktop environments for any user interaction or background execution. \\
        \bottomrule
    \end{tabularx}
\end{table}

\FloatBarrier

With the operational boundaries established, the Functional Requirements (FR) define the explicit behaviors, interactions, and data-processing capabilities the system must exhibit. These requirements define the \enquote{what} of the system, strictly abstracting away internal implementation details to focus entirely on user-facing features, catalog lifecycle management, and logical orchestration. The complete functional specification is presented in Table \ref{tab:functional_reqs}.

\begin{table}[htbp]
    \centering
    \caption{Functional Requirements (FR)}
    \label{tab:functional_reqs}
    \renewcommand{\arraystretch}{1.3}
    \begin{tabularx}{\textwidth}{@{} l l X @{}}
        \toprule
        \textbf{ID} & \textbf{Requirement} & \textbf{Description} \\
        \midrule
        FR-1 & Command Generation & The system must accept natural language queries and return a syntactically correct command-line string comprising the base tool, relevant option flags, and placeholders for user-specific positional parameters. \\
        FR-2 & Non-Execution Boundary & The system must strictly output the generated command as text and never autonomously execute it in the host shell. \\
        FR-3 & Authenticated Ingestion & The system must provide a programmatic interface to ingest and index new tool catalogs, utilizing an authentication or cryptographic authorization mechanism to prevent unauthorized manipulation. \\
        FR-4 & Catalog Lifecycle Management & The system must provide authenticated interfaces allowing authorized entities to list, update, and remove previously ingested tool catalogs. \\
        FR-5 & State Persistence & The system must persist all ingested catalogs and computed retrieval states locally, ensuring immediate availability across system reboots and terminal sessions without requiring re-ingestion. \\
        FR-6 & Syntactical Validation & The system must logically evaluate generated commands to ensure no mutually exclusive flags are recommended simultaneously, and that any required positional ordering is strictly respected. \\
        FR-7 & Interactive Disambiguation & If a query is ambiguous, triggers a flag conflict, or falls outside the domain of ingested catalogs, the system must halt and prompt the user for clarification rather than returning an invalid command. \\
        FR-8 & User Interface & The system must expose a user interface for all user-facing operations, including query submission, catalog management, and interactive disambiguation. \\
        FR-9 & Configuration Management & The system must provide an accessible mechanism for the user to view and persistently modify local system preferences without requiring direct manipulation of the underlying storage mechanism or compiled source code. \\
        \bottomrule
    \end{tabularx}
\end{table}

While Functional Requirements dictate what the system must do, the Non-Functional Requirements (NFR) define the standard of quality to which the system must adhere. These attributes establish the rigorous performance budgets, architectural modularity paradigms, security perimeters, and latency thresholds required to ensure the tool remains a viable, high-performance alternative to cloud-based assistants. The quantifiable metrics and structural constraints governing the system's execution quality are enumerated in Table \ref{tab:non_functional_reqs}.

\FloatBarrier
\renewcommand{\arraystretch}{1.3} % You can safely increase this again for readability
\begin{xltabular}{\textwidth}{@{} l >{\raggedright\arraybackslash}p{2.8cm} >{\raggedright\arraybackslash}X @{}}

    % --- 1. FIRST PAGE HEADER ---
    \caption{Non-Functional Requirements (NFR)}
    \label{tab:non_functional_reqs} \\
    \toprule
    \textbf{ID} & \textbf{Requirement} & \textbf{Description} \\
    \midrule
    \endfirsthead

    % --- 2. SUBSEQUENT PAGE HEADERS ---
    \caption*{Table \ref{tab:non_functional_reqs}: Non-Functional Requirements (Continued)} \\
    \toprule
    \textbf{ID} & \textbf{Requirement} & \textbf{Description} \\
    \midrule
    \endhead

    % --- 3. BOTTOM OF PAGE FOOTER (When breaking to a new page) ---
    \midrule
    \multicolumn{3}{r}{\textit{Continued on next page \dots}} \\
    \endfoot

    % --- 4. ABSOLUTE LAST FOOTER (End of the entire table) ---
    \bottomrule
    \endlastfoot

    % --- DATA ROWS ---
    NFR-1 & Query Latency & The system must return a completed command recommendation in under 1000ms from the moment the query is submitted. \\
    
    NFR-2 & Cold Startup Latency & The application must initialize and be ready to process input in under $1000ms$. \\
    
    NFR-3 & Memory Bounding & The peak RAM utilization of the system process must never exceed $1024 MB$ at any time during operation. \\
    
    NFR-4 & Latency Scalability & The system must maintain its $< 1000ms$ query latency when the persistent state is scaled to hold definitions for up to ** Average number of CLI tools installed on a linux machine ** distinct tools. \\
    
    NFR-5 & Retrieval Quality & The system must achieve a minimum Macro-Averaged F1-Score of **[Target]** and a Jaccard Similarity of **[Target]** against the established evaluation dataset. \\
    
    NFR-6 & Storage Modularity & The system architecture must strictly decouple the core application logic from the persistent state mechanisms. It must be possible to completely replace the underlying storage backend without modifying the domain logic, retrieval algorithms, or user interfaces. \\

    NFR-7 & Search and Matching Modularity & The system architecture must isolate all specific search and matching mechanisms behind abstract interfaces.\\
    
    NFR-8 & Similarity and Rank Aggregation & The system must support the multi-strategy evaluation of candidate command-line options against the user's natural language query. It must provide an internal mechanism to mathematically normalize and fuse disparate similarity scores from independent matching algorithms into a single, unified ranking before outputting the finalized result.\\

    NFR-9 & Linux Deployment & The system must be deployable as a self-contained artifact for Linux environments, requiring no pre-installed language runtimes or package managers on the host machine. \\
    
    NFR-10 & IPC Network Isolation & Any \ac{IPC} mechanisms utilized by the system must be strictly bound to local OS constructs, explicitly denying any non-local network access. \\
    
    NFR-11 & State Access Control & The system must utilize host OS access control mechanisms to ensure its persistent state data is logically isolated and protected from unauthorized modification by other non-privileged users. \\
    
    NFR-12 & Concurrency Resilience & The system must safely process simultaneous execution requests from multiple terminal instances without causing deadlocks, process crashes, or state corruption. \\
    
    NFR-13 & Input Sanitization & The system must sanitize all user inputs and programmatic payloads to prevent injection attacks against the underlying persistent state mechanism. \\
    
    NFR-14 & Opt-In Diagnostic Logging & By default, any diagnostic logs generated by the system must be strictly scrubbed of the user's raw natural language queries. Unredacted query logging must require explicit user opt-in. \\

\end{xltabular}

\FloatBarrier

%\subsubsection{Architectural Pattern}
%\label{sec:architecture_philosophy}

%The design of the system is fundamentally driven by the non-functional requirements governing latency, memory footprint, and modularity. Specifically, the system must process natural language queries in under 1000ms (NFR-1, NFR-4) within a 1024 MB memory limit (NFR-3), 

%**while ensuring that the core algorithmic retrieval logic remains strictly decoupled from volatile \ac{I/O} operations and storage mechanisms (NFR-6). ** Umschreiben, ggf. Requirement dass storage backend austauschbar sein muss

%To satisfy these constraints, a traditional Layered (N-Tier) Architecture was discarded. While a layered approach neatly categorizes concerns into presentation, business, and data tiers, it introduces a transitive top-down dependency structure**SOURCE**. In such a model, the core algorithmic logic is inherently coupled to the specific implementation of the underlying data access layer. 

%If the local storage mechanism were to be replaced, the business logic would require modification, directly violating the modularity constraint (NFR-6). ** UMSCHREIBEN, NFR-6 bezieht sich auf die retrieval modells (Embedding \& Bm25), ggf. NFR-6 umschreiben dass das auch darin konkret reflectiert ist! Ich möchte den Keyword matching und das embedding modell austauschen können. Ich möchte auch das storage backend tasuchen können, also ggf. 2 Requirements draus machen.

%To decouple the algorithmic core from the storage layer, the system relies on the Dependency Inversion Principle. While modern dependency-inverting paradigms such as Clean Architecture \cite{martin2012clean}**SOURCES** and Onion Architecture \cite{palermo2008onion}**SOURCES** offer robust domain isolation, they prescribe concentric internal layers that mandate extensive \ac{DTO} mapping and rigid interface boundaries. For a localized \ac{CLI} operating under strict computational and latency budgets (NFR-1, NFR-3), this enterprise-level boilerplate introduces unjustifiable computational overhead and complexity. ** Ergänzen dass die komplexität der anwendung (für die instandhaltung/erweiterung) dadurch inhärent steigt

%Therefore, this system adopts a \textbf{Hexagonal Architecture}, also known as the Ports and Adapters pattern \cite{cockburn2005hexagonal}**SOURCES**. This pattern provides the necessary macro-level boundary control without dictating internal bureaucratic layers. ** Allowing to focus on the actual implementation ** The architecture is divided into two strict conceptual zones:

%\begin{itemize}
%    \item \textbf{The Core (Inside):} This encapsulates the mathematical retrieval models, vector search logic, and domain entities (e.g., Commands, Flags, and Catalogs). The Core possesses zero knowledge of the external world. It dictates its storage and interaction needs purely through abstract interfaces (Ports).
%    \item \textbf{The Infrastructure (Outside):} This encompasses all external I/O mechanisms, including the user-facing CLI terminal (Driving Adapter) and the local persistent state database (Driven Adapter). 
%\end{itemize} 
%** Das passt so nicht ganz, Das modell und der keyword matching algorithmus müsse nauch austasuchbar sein! Daher müssen diese auch als "plugins" definiert werden.

%By implementing data-access and presentation interfaces at the edge of the Core domain,
%** Include the Embedding Modell and the Keyword matching algorithm here!**
%the external mechanisms are forced to depend on the Core rather than the inverse. This guarantees that the core natural language processing and hybrid retrieval engine can be compiled, tested, and executed entirely independent of the chosen database technology or host operating system terminal, successfully fulfilling all architectural constraints.
%** Rewrite this entire paragraph!**

\subsection{Architectural Pattern}
\label{sec:architecture_philosophy}

The design of the system is fundamentally driven by the non-functional requirements governing latency, memory footprint, and modularity. Specifically, the system must process natural language queries in under 1000ms (NFR-1, NFR-4) within a 1024 MB memory limit (NFR-3), while ensuring that the core domain logic remains strictly decoupled from volatile \ac{I/O} operations, allowing both the persistent storage backend (NFR-6) and the specific search and matching algorithms (NFR-7) to be seamlessly replaced.

To satisfy these constraints, a traditional Layered (N-Tier) Architecture was discarded. While a layered approach neatly categorizes concerns into different tiers (e.g. presentation, business and data), it introduces a transitive top-down dependency structure \cite{richards2020fundamentals}. As Richards and Ford assert, layers in this architecture remain isolated only if the contracts between them remain unchanged. However, in a standard layered model, the data access layer and external libraries define these contracts. Consequently, the core business logic is transitively coupled to the paradigms of the infrastructure. If the local storage technology or the specific text-matching algorithms were fundamentally replaced, the cascading changes to the underlying contracts would inevitably force direct modifications within the business logic. This top-down coupling directly violates the system's modularity constraints (NFR-6, NFR-7).

To decouple the algorithmic core from the storage and execution components, the system relies on the Dependency Inversion Principle. While modern dependency-inverting paradigms such as Clean Architecture \cite{martin2017clean} and Onion Architecture \cite{palermo2008onion} offer robust domain isolation, they prescribe concentric internal layers that mandate extensive \ac{DTO} mapping and rigid interface boundaries. For a localized command-line utility, operating under strict computational and latency budgets (NFR-1, NFR-3, NFR-4), this enterprise-level boilerplate introduces unjustifiable computational overhead. Furthermore, it inherently increases the structural complexity of the codebase, making future maintenance and the extension of new features unnecessarily burdensome for a utility application.

Therefore, this system adopts a \textbf{Hexagonal Architecture}, also known as the Ports and Adapters pattern \cite{cockburn2005hexagonal}. This pattern provides the necessary macro-level boundary control without dictating internal bureaucratic layers, allowing developers to maintain focus on the actual implementation of the core feature set. The architecture is divided into two strict conceptual zones:

\begin{itemize}
    \item \textbf{The Core (Inside):} This encapsulates the domain entities, the command validation (FR-6), and the mathematical normalization and score aggregation logic (NFR-8). The Core possesses zero knowledge of the external world, it dictates its storage and matching needs purely through abstract interfaces (Ports).
    \item \textbf{The Infrastructure (Outside):} This encompasses all external adapters that fulfill the Core's interfaces. This includes the user-interface, the storage backend, as well as the specific execution engines for the command option evaluation.
\end{itemize}
\FloatBarrier

By implementing interface ports at the absolute boundary of the Core, the external mechanisms -- whether they are the user interface, storage backend, or the command option evaluation mechanisms -- are forced to depend on the Core rather than the inverse. This guarantees that the core natural language orchestration and rank aggregation math can be compiled, tested, and executed entirely independently of the chosen storage backend or matching technologies, successfully fulfilling all modularity constraints.

\subsection{Static Architecture}

\subsubsection{System Context Model (Level 1)}
\label{sec:system_context}

To formalize the operational boundaries of the software and verify the isolation constraints, a Level 1 System Context diagram was modeled using the C4 standard \cite{brown2026c4}. As illustrated in Figure~\ref{fig:arch_level_1}, the proposed tool is treated as a unified, opaque system operating entirely within a headless Linux environment (ENV-4). This high-level abstraction defines the absolute perimeter of the application, explicitly identifying the external actors and systems permitted to interact with it. 

\begin{figure}[htbp]
    \centering
    \includegraphics[width=0.8\textwidth]{abbildungen/HLA-Diagram-Level-1.jpg} 
    \caption{Level 1 System Context Model}
    \label{fig:arch_level_1}
\end{figure}
\FloatBarrier

The system interacts with two primary driving actors that initiate operations:
\begin{itemize}
    \item \textbf{The End-User:} Interacts with the system by submitting natural language queries and configuration parameters (FR-1, FR-9). In turn the End-User receives the command best fitting their query and disambiguation prompts (FR-7, FR-9).
    \item \textbf{The Catalog-Maintainer:} Represents either a human developer or an application developed by them. This actor interfaces with the system via an authenticated programmatic boundary to ingest new or updated, structured tool catalogs, receiving synchronous validation receipts upon success or failure (FR-3). The Catalog-Maintainer also has the ability to delete their catalogs usign the authenticated interface.
\end{itemize}
\FloatBarrier

Furthermore, the system relies on a single driven external system: the \textbf{Host \ac{OS}}. The application initiates outward requests to the host operating system to read and write its persistent state. While the system manages application-level authentication, it defers to the underlying \ac{OS} file permission mechanisms to enforce \ac{OS}-level access control for all persisted data (NFR-11). 

Most critically, the Context Model serves as visual and structural proof of the system's privacy and offline execution limitations (ENV-2). It also demonstrates that the zero third-party daemon reliance (ENV-3) is considered. The defined perimeter strictly excludes any outbound network or third party daemon dependencies. By restricting the attack surface and data flow strictly to the local host operating system and authorized standard inputs, the architecture guarantees that all user queries and proprietary tool descriptions remain entirely localized.

\subsubsection{System Context Model (Level 2)}
\label{sec:container_architecture}

Following the C4 modeling standard \cite{brown2026c4}, the Level 2 Container Diagram penetrates the opaque system boundary established in Level 1 to define the high-level, independently executable logical units. While the system is deployed to a localized, offline environment, it is structurally partitioned into two distinct containers. This separation formally illustrates the boundary between volatile computational execution and persistent state management, a necessary precursor to satisfying the storage modularity constraint (NFR-6).

As illustrated in Figure~\ref{fig:arch_level_2}, the system boundary encapsulates the following containers:

\begin{itemize}
    \item \textbf{The \ac{CLI} Executable:} A compiled, standalone binary (NFR-9) that contains the system's core logic. It acts as the sole active computational process.
    \item \textbf{The Local Database:} A daemonless data store strictly responsible for the persistent retention of system and configuration data. 
\end{itemize}
\FloatBarrier

\begin{figure}[htbp]
    \centering
    \includegraphics[width=0.8\textwidth]{abbildungen/HLA-Diagram-Level-2.jpg} 
    \caption{Level 2 Container Architecture}
    \label{fig:arch_level_2}
\end{figure}
\FloatBarrier

The inclusion of the system boundary (represented by the dashed perimeter) explicitly clarifies how external actors interface with the internal components. Both the \textbf{End-User} and the \textbf{Catalog-Maintainer} bypass the generic system perimeter to interact exclusively and directly with the \ac{CLI} Executable, submitting queries and ingestion payloads, respectively.

Crucially, the diagram illustrates the specific dependency mechanics of an embedded data persistence architecture. Because the persistence mechanism must be daemonless, it operates as an embedded library, hence the data persistence initiates no outward  connections. The \ac{CLI} Executable maintains a strict, unidirectional dependency on the persistent storage, reading and writing state as needed. Consequently, it is the \ac{CLI} Executable--not the persistent storage itself--that initiates all direct interactions with the \textbf{Linux Host \ac{OS}} to delegate access control and execute the underlying file \ac{I/O} system calls.

\subsubsection{System Context Model (Level 3)}
\label{sec:component_architecture}

To detail the internal logical arrangement of the compiled application, the Level 3 Component Diagram decomposes the \ac{CLI} Executable container into its structural building blocks. In this architecture, Level 3 serves as the concrete realization of the Hexagonal (Ports and Adapters) philosophy declared in Section~\ref{sec:architecture_philosophy}. It explicitly delineates the boundaries between the pure domain logic, the abstract interface contracts (Ports), and the pluggable execution infrastructure (Adapters).

As modeled in Figure~\ref{fig:arch_level_3}, the internal architecture strictly enforces the Dependency Inversion Principle. The components are categorized into three conceptual rings of execution:

\begin{figure}[htbp]
    \centering
    \includegraphics[width=0.8\textwidth]{abbildungen/HLA-Diagram-Level-3.jpg} 
    \caption{Level 3 Component Architecture detailing the Hexagonal boundaries}
    \label{fig:arch_level_3}
\end{figure}
\FloatBarrier

\paragraph{1. The Driving Adapters (Inbound Interface)}
Positioned at the perimeter, the driving adapters are responsible for receiving external actor input and translating it into domain-specific requests. 
\begin{itemize}
    \item The \textbf{\ac{CLI} Controller} parses standard input from the End-User, manages the interactive disambiguation loop (FR-7), and formats the output (FR-8).
    \item The \textbf{Ingestion API Handler} provides the authenticated boundary for receiving structured catalogs from the Catalog-Maintainer (FR-3). 
\end{itemize}
\FloatBarrier
Crucially, these adapters do not invoke the application core directly; rather, they depend on abstract inbound ports (\textbf{UserCommandPort} and \textbf{CatalogIngestionPort}).

\paragraph{2. The Isolated Core and Interface Ports}
The architectural center of gravity consists entirely of pure computational logic and the definition of the boundary interfaces. It contains zero references to external frameworks or database libraries.
\begin{itemize}
    \item \textbf{The Inbound Dependency Inversion:} The \textbf{Query Orchestrator} and the \textbf{Catalog Lifecycle Manager} serve as the primary use-case interactors. **They physically \textit{implement} the inbound ports.** CHECK IF THIS CORRECT! ** Consequently, their compile-time dependency arrows point inward toward the port interfaces, proving that the core dictates the execution contracts to the driving adapters.
    \item \textbf{Domain Mathematics \& Rules:} The \textbf{Similarity \& Rank Aggregator} executes the mathematics required for the unified ranking (NFR-8). To prevent data starvation while maintaining domain purity, the Query Orchestrator fetches definitions via the storage port and passes them in-memory to the \textbf{Syntactical Validator}. This allows the validator to evaluate command validity (FR-6) as a pure function without requiring its own \ac{I/O} capabilities.
\end{itemize}
\FloatBarrier

\paragraph{3. The Driven Adapters (Outbound Infrastructure)}
When the core requires external data or heavy computational execution, it calls its internally defined outbound interfaces (\texttt{StoragePort} and \texttt{MatchingStrategyPort}). The driven adapters exist outside the core to physically fulfill these requests.
\begin{itemize}
    \item The \textbf{Persistence Adapter} implements the \textit{StoragePort} interface, translating domain entities into the strucutres required by the underlying storage mechanism and executing the underlying host \ac{OS} file operations (NFR-6, NFR-11).
    \item The \textbf{Matching Strategy Adapters [1..*]} fulfill the matching interface. By utilizing the extension \texttt{1..n}, the diagram shows that the core is entirely agnostic to the number or type of search algorithms employed (NFR-7). 
\end{itemize}
\FloatBarrier

Ultimately, this Level 3 configuration guarantees that all dependencies point inward toward the abstract ports. Because the core components only depend on interfaces they themselves define, the underlying storage mechanisms or scoring mechanisms can be entirely replaced without modifying the central orchestration or mathematical logic.

\subsubsection{Omission of C4 Level 4}

While the C4 framework includes a fourth level for mapping individual components to code-level constructs, it is treated as an optional level of detail within this specification. As noted by the C4 standard, this depth is typically reserved for only the most complex or critical components to help tell a specific implementation story \cite{brown2026c4}. Because class structures are highly volatile and often available on-demand via tooling options, documenting them here would risk creating brittle artifacts. Instead, this architecture relies on the Level 3 component boundaries to provide sufficient rigor, leaving low-level attributes and methods to be automatically generated where deemed necessary.

\subsection{Dynamic Architecture (System Behavior)}
\label{sec:dynamic_architecture}

While the static architecture defined in Section~\ref{sec:component_architecture} establishes the strict structural boundaries of the system, it does not describe the runtime execution logic. This section models the dynamic behavior of the \ac{CLI} application using \ac{BPMN} 2.0 standard notation. To prevent visual bloating, some diagrams have been structured hierarchically by adding subprocess diagrams.

\paragraph{Architectural Execution Model (The Stateless Lifecycle)}
\label{sec:execution_model}

To strictly satisfy the \enquote{Single Linux Deployment Artifact} (NFR-9) and \enquote{Zero Third-Party Daemon} (ENV-3) constraints, the system is designed as a purely stateless executable. The application does not rely on background listeners or idle memory allocation. Instead, every interaction follows a strictly bounded lifecycle: the process is initialized by the host \ac{OS}, allocates memory Just-In-Time (JIT), executes the requested domain logic, and immediately terminates. 

This daemonless approach guarantees that \ac{RAM} limits (NFR-3) are only consumed during active execution and that only one executable has to be installed.

\subsubsection{Primary Query Resolution Flow}
\label{sec:primary_query_flow}

The primary functional purpose of the system is to translate natural language into executable terminal commands (FR-1). Figure~\ref{fig:bpmn_high_level} illustrates the High-Level Systems Overview of this interaction.

\begin{figure}[htbp]
    \centering
    \includegraphics[width=0.8\textwidth]{abbildungen/BPMN-1-High-Level.jpg} 
    \caption{BPMN High-Level Systems Overview for Query Resolution}
    \label{fig:bpmn_high_level}
\end{figure}
\FloatBarrier

The flow begins when the End-User submits a query. The core orchestration layer  initializes the necessary matching engines before tokenizing the query and dispatching it to the subprocesses. It explicitly handles ambiguities by utilizing disambiguation prompts to the user.

\paragraph{Subprocesses}
As described before, the process in Figure~\ref{fig:bpmn_high_level} has been simplified to ensure the diagram remains easily comprehensible. The following diagrams aim to provide more detailed insights into the previously simplified subprocesses.

\paragraph{Tool and Option Determination}

The system utilizes the pluggable matching adapters (defined in section~\ref{sec:component_architecture}) to match the tokenized data against the data persisted in the data store. If the highest resulting score remains below the minimum confidence threshold, the process immediately aborts evaluation and returns a \enquote{Tool Undetermined} state.

\begin{figure}[htbp]
    \centering
    \includegraphics[width=0.8\textwidth]{abbildungen/BPMN-2-Sub-High-Level.jpg} 
    \caption{BPMN High-Level Systems Overview for Query Resolution}
    \label{fig:bpmn_2_sub_high_level}
\end{figure}
\FloatBarrier

\paragraph{Filtering of Candidate Options}

To prevent the hallucination of low-quality results, the system applies a strict filter. If the highest match score does not exceed a confidence threshold, it prunes all options. Otherwise, it calculates the percentage drop between the ranked candidates, severing the list where the cliff first exceeds a threshold.

\begin{figure}[htbp]
    \centering
    \includegraphics[width=0.8\textwidth]{abbildungen/BPMN-3-Sub-High-Level.jpg} 
    \caption{BPMN High-Level Systems Overview for Query Resolution}
    \label{fig:bpmn_3_sub_high_level}
\end{figure}
\FloatBarrier

\paragraph{Command Verification}

The candidate tool and options are validated against the catalog schema (FR-6) to ensure mutually exclusive flags are not combined and that positional ordering is respected. Successfully built commands trigger the termination of the subprocess and are returned to the user.

\begin{figure}[htbp]
    \centering
    \includegraphics[width=0.8\textwidth]{abbildungen/BPMN-4-Sub-High-Level.jpg} 
    \caption{BPMN High-Level Systems Overview for Query Resolution}
    \label{fig:bpmn_4_sub_high_level}
\end{figure}
\FloatBarrier

\paragraph{The Interactive Disambiguation Loop}
If the Tool and Option Determination subprocess fails to achieve the required confidence threshold, or if the Command Verification subprocess encounters a validation error, an interactive disambiguation loop (FR-7) is triggered. The system issues a clarification request to the End-User, and awaits a rewritten query. Crucially, this loop routes back to the tokenization phase, explicitly bypassing the engine initialization phase to minimize latency on subsequent iterations.
** FIX ALL CAPTIONS FOR FIGURES! **
% ### corrected till here


\subsubsection{Administrative Execution Flows}
\label{sec:administrative_flows}

Beyond query resolution, the system must securely handle state mutations initiated by both users and external maintainer applications.

\paragraph{Catalog Lifecycle Management}
External maintainers inject new tool definitions (FR-3, FR-4). As modeled in the Catalog Management flow (Figure~\ref{fig:bpmn_5_high_level}), the system first intercepts the request to authenticate and authorize the action. Once authorized, an exclusive gateway routes the payload to either the ingestion or deletion logic. If a catalog is being created or updated, the system preprocesses the ingested catalog data before persisting them to the data store. Regardless of the action, the system guarantees a synchronous success or failure receipt before the process terminates, ensuring maintainers can rely on a status message.

\begin{figure}[htbp]
    \centering
    \includegraphics[width=0.8\textwidth]{abbildungen/BPMN-5-High-Level.jpg} 
    \caption{BPMN High-Level Systems Overview for Query Resolution}
    \label{fig:bpmn_5_high_level}
\end{figure}
\FloatBarrier

\paragraph{Configuration Management}
For local system adjustments (FR-9), such as modifying thresholds or enabling diagnostic logging, the system provides a linear execution path. The user is authenticated and authorized. They then receive the configuration options and subsequently send back configuration data. This configuration data then gets persisted after which the \ac{CLI} terminates.

\begin{figure}[htbp]
    \centering
    \includegraphics[width=0.8\textwidth]{abbildungen/BPMN-6-High-Level.jpg} 
    \caption{BPMN High-Level Systems Overview for Query Resolution}
    \label{fig:bpmn_6_high_level}
\end{figure}
\FloatBarrier

\subsection{Implementation}

% - Rust

% Beschreibung der Module und wie sie ineinandergreifen (Diagram?)

% Ggf. eine beschreibung wie man besonders effectiv die Beschreibungen für das Embedding formuliert.

% tar, mv, cp, 