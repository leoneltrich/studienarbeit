\section{Discussion}

\subsection{Validity Concerns}

in the evaluation there were only 3 tools so the likelyhood of the wrong one being retrieved was significantly lower than in a standard system.

The effectiveness of the results depends heavily on the catalog being optimized

if there is someone taking the time to write an optimized catalog they might as well just write docs for it

Only bi encoder embedding models were used

Qwen prompts, only 3 variations tried

\subsection{Answers To The Research Questions}